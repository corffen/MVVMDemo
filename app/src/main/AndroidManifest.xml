<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.corffen.mvvmdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BasicActivity" />
        <activity android:name=".ViewBindingDataActivity" />
        <activity android:name=".ViewBindingData2Activity" />
        <activity android:name=".ClickActivity" />
        <activity android:name=".ClickWithViewModelActivity" />
        <activity android:name=".MultiPropertyActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".BindingAdapterActivity" />
        <activity android:name=".RvActivity" />
    </application>

</manifest>